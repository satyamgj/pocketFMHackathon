# --- Core pipeline (extraction, recap, judge) ---
openai>=1.50.0            # GPT API client
pydantic>=2.6.0             # schema validation for BibleEntry, RecapScript, etc.
fastapi
uvicorn[standard]
streamlit

# --- One-time audio -> transcript step (run BEFORE the hackathon) ---
faster-whisper>=1.0.0       # local ASR, only needed for a one-off transcript-generation script

# --- Dev convenience ---
python-dotenv>=1.0.0         # loads ANTHROPIC_API_KEY from .env

